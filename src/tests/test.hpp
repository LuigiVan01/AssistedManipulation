#pragma once

#include <stdexcept>
#include <iostream>
#include <fstream>
#include <filesystem>
#include <string>

template<typename Base>
class Test : public Base
{
public:

    Test(const std::string &name, )

private:

};
